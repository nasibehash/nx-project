import { Route } from '@angular/router';

export const appRoutes: Route[] = [
  // { path: ':lang', component: HomeComponent }, // Handles routes with language code
  // { path: '', redirectTo: '/en', pathMatch: 'full' } // Default to English
];
